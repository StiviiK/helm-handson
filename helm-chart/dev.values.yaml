replicaCount: 1

image:
  tag: "stable"

ingress:
  enabled: true
  className: "nginx"
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
  hosts:
    - host: demo.mirix.io
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: demo.mirix.io
      hosts:
        - demo.mirix.io

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi